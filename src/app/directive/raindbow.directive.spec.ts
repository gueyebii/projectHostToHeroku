import { RaindbowDirective } from './raindbow.directive';

describe('RaindbowDirective', () => {
  it('should create an instance', () => {
    const directive = new RaindbowDirective();
    expect(directive).toBeTruthy();
  });
});
